# Database schema (PostgreSQL)

> **Lead is the only writer of this file.** Backend proposes schema changes alongside the migration file; devops applies the migration. Lead writes the entry here once the migration is applied.
>
> This file mirrors the live schema. If a migration is generated but not applied, note it under "Pending migrations" — do not move it into "Tables" until applied.

## Conventions

- **Migration tool:** `Alembic` (async via `async_engine_from_config`)
- **Naming:** `snake_case` for tables and columns, plural table names (`projects`, `tasks`)
- **Primary key:** `BigInteger` autoincrement (`GENERATED BY DEFAULT AS IDENTITY`) — UUIDs intentionally avoided (see [decisions.md](decisions.md), 2026-05-04)
- **Timestamps:** `created_at timestamptz NOT NULL DEFAULT now()`, `updated_at timestamptz NOT NULL DEFAULT now()` — router bumps `updated_at` manually on PATCH (server_default only fires on INSERT). Could move to a PG trigger later if more write paths appear.
- **Soft delete:** yes — every business table has `status SMALLINT NOT NULL DEFAULT 1 CHECK (status IN (0, 1))` where `1=active`, `0=deleted`. Application code never issues SQL DELETE; the audit trigger captures flag flips as `'U'`. Hard DELETE is reserved for manual psql cleanup. See [decisions.md](decisions.md) 2026-05-05 entry. The 1-5 lifecycle column on `tasks` was renamed `status → process_status` in migration `0002_soft_delete_and_lead` (2026-05-08) to free `status` for the uniform soft-delete name.
- **Audit:** `tasks_history` is populated by PG trigger; application code does NOT insert there

## Tables

<!--
Template for a new table:

### <table_name>
**Purpose:** <one line>

| column        | type         | constraints                               | notes |
|---------------|--------------|-------------------------------------------|-------|
| id            | uuid         | PK, default `gen_random_uuid()`           |       |
| <field>       | <type>       | <constraints>                             |       |
| created_at    | timestamptz  | NOT NULL, default `now()`                 |       |
| updated_at    | timestamptz  | NOT NULL, default `now()`                 |       |

**Indexes:**
- `<index_name>` on `(col_a, col_b)` — <reason>

**Foreign keys:**
- `<fk_col>` → `<other_table>(id)` ON DELETE <CASCADE|SET NULL|RESTRICT>
-->

### projects
**Purpose:** Registered projects (one entry per stack on disk; `is_active` is a free boolean post-#694 Phase 2 — each Claude Code session binds to a project by name at bootstrap, so multiple live projects may simultaneously carry `is_active=true`).

| column        | type         | constraints                                          | notes |
|---------------|--------------|------------------------------------------------------|-------|
| id            | bigint       | PK, identity                                         |       |
| name          | text         | NOT NULL                                             | uniqueness via partial index `ux_projects_name_active` |
| description   | text         | NULL                                                 |       |
| paths_web     | text         | NOT NULL                                             |       |
| paths_api     | text         | NOT NULL                                             |       |
| paths_db      | text         | NOT NULL                                             |       |
| stack_web     | text         | NULL                                                 |       |
| stack_api     | text         | NULL                                                 |       |
| stack_db      | text         | NULL                                                 |       |
| config        | jsonb        | NOT NULL DEFAULT `'{}'`                              | houses `standards` mapping |
| is_active     | bool         | NOT NULL DEFAULT `false`                             |       |
| team          | text         | NOT NULL DEFAULT `'dev'`, CHECK `team IN ('dev','novel')` (named `ck_projects_team_valid`) | drives subagent roster (see scaffold service + `.claude/teams/<team>.md`). Renamed from `lead` by `0004_rename_lead_to_team` (2026-05-09); the `.claude/leads/` → `.claude/teams/` directory rename followed in Phase 2.5b2 (same date). |
| auto_run_consent_at | timestamptz | NULL                                          | NULL = no consent; non-null = user consented at this timestamp to auto-headless runs in this project. Required (app-layer validator, #483-B) for any task in the project to use `run_mode = 'auto_headless'`. UI-driven grant via typed-acknowledgment endpoint (see decisions.md 2026-05-09 Step-2 entry). Added by `0005_run_mode_and_consent`. |
| working_path  | text         | NULL                                                 | Single project-root path on the dev machine. Orthogonal to `paths_web/api/db` (which are LANE-specific sub-paths within a project); `working_path` is the project ROOT that wraps them. Advisory metadata — no host-side validation (API host may differ from where the path is meaningful). `min_length=1` at Pydantic boundary. Added by `0012_projects_path_repo_ovr` (Kanban #777, 2026-05-12). |
| working_repo  | text         | NULL                                                 | Free-form repo identifier — URL (https/ssh) or local path. No regex enforced; `min_length=1` at Pydantic only. Added by `0012`. |
| agent_overrides | jsonb      | NOT NULL DEFAULT `'{}'`                              | Per-project subagent model routing. Keys: role names matching `^[a-zA-Z0-9_-]{1,64}$` (Pydantic `field_validator`); values: one of `'haiku'`, `'sonnet'`, `'opus'` (Pydantic Literal). PATCH-null normalized to `{}` by router (NOT SQL NULL) — `server_default` fires only on INSERT; the app layer guarantees a dict on every write. ProjectRead types this as `dict[str, Any]` (always-dict, value-tolerant). Added by `0012`. |
| sources       | jsonb        | NULL DEFAULT `'[]'::jsonb`, CHECK `jsonb_array_length(sources) <= 20` (named `ck_projects_sources_length`) | Per-project curated source list — URLs / refs / doc anchors / repos shown in the UI popover and consumed by dev-researcher. Element shape (`{url: str (1..2000), label?: str (1..200), kind?: Literal['doc','spec','repo','dashboard','other']}`) validated at the API boundary by Pydantic `SourceEntry` (`extra='forbid'`; permissive URL-shape validator: scheme:// OR Unix/Windows absolute path) — no DB CHECK on element shape (same precedent as `config`, `agent_overrides`, `tasks.acceptance_criteria`). NULL is treated identically to `[]` by the app layer. PATCH-null normalized to `[]` by router (parity with `agent_overrides` WARN-1 Option A) — DB column IS nullable but the app layer treats NULL identically to `[]`, so the response boundary contract is "always a list, never null". `ProjectRead.sources` typed as `list[dict[str, Any]]` (always-list, value-tolerant) — same precedent as `agent_overrides`. Pydantic `max_length=20` on the list (422 boundary) + DB CHECK (defense-in-depth). PG 16 metadata-only ADD COLUMN on apply — every pre-existing row reads `[]`. CHECK tolerates NULL via SQL three-valued logic. Added by `0020_projects_sources` (Kanban #778, 2026-05-13); ORM/Pydantic/router wire-up landed in the same Kanban task on 2026-05-13. |
| status        | smallint     | NOT NULL DEFAULT 1, CHECK `status IN (0, 1)` (named `ck_projects_status_valid`) | soft-delete flag |
| created_at    | timestamptz  | NOT NULL DEFAULT `now()`                             |       |
| updated_at    | timestamptz  | NOT NULL DEFAULT `now()`                             |       |

**Indexes:**
- `ux_projects_name_active` partial UNIQUE on `(name)` WHERE `status = 1` — name re-usable after soft delete
- ~~`ux_projects_active_one` partial UNIQUE on `(is_active)` WHERE `is_active IS TRUE AND status = 1`~~ — DROPPED 2026-05-10 by `0006_drop_active_one` (Kanban #694 Phase 2; session-scoped active means multiple rows may now carry `is_active=true` simultaneously)
- `ix_projects_status` on `(status)` — keeps default-filter selective

### tasks
**Purpose:** Kanban tasks scoped to a project.

| column         | type        | constraints                                                         | notes |
|----------------|-------------|---------------------------------------------------------------------|-------|
| id             | bigint      | PK, identity                                                        |       |
| project_id     | bigint      | NOT NULL, FK `projects(id)` ON DELETE CASCADE                       |       |
| parent_task_id | bigint      | NULL, FK `tasks(id)` ON DELETE CASCADE, CHECK `parent_task_id IS NULL OR parent_task_id <> id` (named `ck_tasks_parent_task_id_not_self`) | self-ref subtask hierarchy (Kanban #238, 2026-05-08). FK CASCADE is defense-in-depth — app never hard-deletes; soft-delete with active children blocks at 409 in the API. |
| title          | text        | NOT NULL                                                            |       |
| description    | text        | NULL                                                                |       |
| process_status | int         | NOT NULL DEFAULT 1, CHECK `process_status IN (1,2,3,4,5,6)` (named `ck_tasks_process_status_valid`) | TaskStatus codes (general.md): 1=TODO, 2=IN_PROGRESS, 3=REVIEW, 4=BLOCKED, 5=DONE, 6=CANCELLED. Renamed from `status` 2026-05-08; CANCELLED=6 added by `0022_tasks_cancelled_and_reason` (Kanban #854, 2026-05-13). `GET /api/tasks` excludes process_status=6 by default; opt-in via `?include_cancelled=true`. |
| priority       | int         | NOT NULL DEFAULT 2, CHECK `priority IN (1,2,3,4)`                   | TaskPriority codes |
| assigned_role  | int         | NULL                                                                | App-layer validates per project team's roster (DB CHECK dropped 2026-05-08). |
| run_mode       | text        | NOT NULL DEFAULT `'manual'`, CHECK `run_mode IN ('manual','auto_pickup','auto_headless')` (named `ck_tasks_run_mode_valid`) | Step 2 AI integration — controls how this task gets picked up by the queue runner. `manual` = user invokes Lead manually (default for backwards-compat); `auto_pickup` = Mode A2 queue runner picks it up in user's Claude Code session (per-Write approval kept); `auto_headless` = Mode B headless worker runs without approval prompts (requires `project.auto_run_consent_at IS NOT NULL` — cross-table validator at app layer, #483-B). Added by `0005_run_mode_and_consent`. |
| status_change_reason | text  | NULL                                                                | Free-form rationale for a `process_status` flip — most commonly cancellation (#854) but available on any transition (Pydantic `min_length=1` → empty string rejects 422; explicit `null` clears; key absent leaves unchanged). PATCH semantics mirror `halt_reason`. Captured into `tasks_history.snapshot` automatically by the existing `tasks_audit_trg` (`to_jsonb(OLD)`) — no separate plumbing. Added by `0022_tasks_cancelled_and_reason` (Kanban #854, 2026-05-13). |
| task_kind      | varchar(8)  | NOT NULL DEFAULT `'ai'`, CHECK `task_kind IN ('ai','human')` (named `ck_tasks_task_kind_valid`) | V3+ T1 — Kanban #706 (2026-05-10). Discriminates AI-runner-driven from human work. **DEFAULT flipped from `'human'` → `'ai'` by `0023_tasks_task_kind_default_ai` (Kanban #858, 2026-05-13) — most tasks are agent-driven; human-kind reserved for tasks requiring a person to answer/decide.** Cross-table rules at `services/task_kind.py` (app-layer; no DB CHECK because cross-column): (1) `task_kind='human'` ↔ `run_mode='manual'` invariant; (2) **interaction-kind coerce (Kanban #858):** when a POST/PATCH lands with resolved `interaction_kind IN ('question','decision')`, the API silently coerces `task_kind='human'` AND `run_mode='manual'` regardless of caller input (Option A — atomic; the HUMAN↔MANUAL invariant never fires because the coerce runs first). Reverse transition (`question/decision → work`) does NOT auto-revert `task_kind`; callers must explicitly PATCH `task_kind='ai'` to revert. Validator fires BEFORE consent gate. Added by `0007_task_kind_and_recurrence`. |
| is_template    | bool        | NOT NULL DEFAULT `false`                                            | Recurrence template flag — `is_template=true` rows are scheduler recipes; spawn (T2) creates child rows pointing back via `spawned_from_task_id`. Templates never have lifecycle PATCHes that drive `process_status`; their PATCH only edits the recipe (rule/timezone/next_fire_at). Added by `0007`. |
| recurrence_rule | varchar(255) | NULL                                                               | Cron expression. Pydantic field validator runs `croniter.croniter.is_valid()`; invalid → 422. Required (with `next_fire_at`) when `is_template=true` — DB CHECK `ck_tasks_template_recurrence_complete` defends. Added by `0007`. |
| recurrence_timezone | varchar(64) | NOT NULL DEFAULT `'UTC'`                                       | IANA TZ name. Pydantic field validator checks `zoneinfo.available_timezones()`; unknown → 422. Cron is timezone-sensitive (`0 9 * * MON` in `Asia/Bangkok` ≠ `UTC`). Added by `0007`. |
| next_fire_at   | timestamptz | NULL                                                                | Scheduler hot-path target. Partial index `ix_tasks_next_fire_at_template` covers `WHERE is_template=true`. T2 scheduler queries `WHERE is_template=true AND next_fire_at IS NOT NULL AND next_fire_at <= now()`. Added by `0007`. |
| spawned_from_task_id | bigint | NULL, FK `tasks(id)` ON DELETE SET NULL                         | Self-ref lineage from spawn fire — child rows point back to their template. ON DELETE SET NULL is defense-in-depth — app never hard-deletes templates (soft-delete only); if a hard delete ever lands (admin path), children stay queryable with NULL lineage rather than corrupted FK. NEVER editable on PATCH (V1 forbids re-parenting; same `model_fields_set` membership pattern as `parent_task_id`). System-set on POST only by the T2 scheduler. Added by `0007`. |
| scheduled_at   | timestamptz | NULL                                                                | One-shot fire path (Kanban #723, 2026-05-10). Mutually exclusive with `is_template=true` — DB CHECK `ck_tasks_scheduled_xor_template` defends; wire-layer XOR (Pydantic + router resolved-final, both 422) catches first. Partial index `ix_tasks_scheduled_at_pending` covers the scheduler hot-path. T2 scheduler queries `WHERE scheduled_at IS NOT NULL AND scheduled_at <= now() AND process_status = 1 AND status = 1 AND is_template = false`. After fire, scheduler clears `scheduled_at` to NULL so a manual ps→1 flip won't re-fire. Stored UTC; serializes `Z` form. Added by `0010_tasks_scheduled_at`. |
| halt_reason    | text        | NULL                                                                | MVP halt signal for full-auto Lead sessions (Kanban #785, 2026-05-12). Free-form text; non-null string = task is halted (auto-pickup query in Kanban #786 MUST skip rows where `halt_reason IS NOT NULL` regardless of `process_status`); NULL = task runs normally. Lead PATCHes to non-empty string to halt per the Kanban #787 decision matrix; user unhalts by PATCH-ing back to NULL. Pydantic enforces `min_length=1` (empty `""` → 422). No CHECK, no index this slice (auto-pickup query filter combines with existing predicates; measured-first index policy). Orthogonal to lifecycle code, same flag pattern as `is_pending` (Kanban #750). Added by `0013_tasks_halt_reason`. |
| interaction_kind | varchar(16) | NOT NULL DEFAULT `'work'`, CHECK `interaction_kind IN ('work','question','decision')` (named `ck_tasks_interaction_kind_valid`) | Headless auto-run gate discriminator (Kanban #830, 2026-05-12). `'work'` = agent-executed task (default, covers all existing rows); `'question'` = auto-run halted, needs a user answer before proceeding; `'decision'` = Option A/B choice requiring human judgment. `TaskInteractionKind` constants mirror `_INTERACTION_KIND_ALL` from migration 0019. Pydantic Literal validated at the API boundary. DB DEFAULT `'work'` backfills all existing rows on migration 0019 apply (PG 16 metadata-only ADD COLUMN — no heap rewrite). `question_payload` is required (Pydantic `model_validator`) when `interaction_kind IN ('question','decision')`; enforced at 422 on POST/PATCH. History capture is free via existing `tasks_audit_trg`. Added by `0019_tasks_interaction_kind`. |
| question_payload | jsonb      | NULL                                                                | Structured question + answer history for `interaction_kind IN ('question','decision')` tasks (Kanban #830, 2026-05-12). Shape: `{question: str, options: list[str]|null, answer_history: [{value, answered_by, answered_at, is_valid, invalidated_reason}]}`. Shape validated at the API boundary by Pydantic `QuestionPayload` / `AnswerHistoryEntry` (no DB CHECK on element shape — same precedent as `acceptance_criteria`). PATCH semantics: full-replace (same as `acceptance_criteria`). Append-only logic for `answer_history` lands in Kanban #832. `answered_at: datetime|null` in each history entry requires `model_dump(mode='json')` before JSONB write (same Kanban #801 pattern as `acceptance_criteria.verified_at`). NULL = no question data; required (422) when `interaction_kind` is `'question'`/`'decision'`. Added by `0019_tasks_interaction_kind`. |
| resume_context | jsonb       | NULL                                                                | Free-form partial-work state stored by Lead when auto-run halts mid-task (Kanban #830, 2026-05-12). Used by the re-spawn brief on resume. No shape constraint at the DB or Pydantic layer — typed as `dict[str, Any] | None`. Full-replace PATCH semantics (same as `acceptance_criteria`). NULL = task has not halted or Lead chose not to persist state. Added by `0019_tasks_interaction_kind`. |
| acceptance_criteria | jsonb  | NULL                                                                | Structured per-criterion exit-criteria tracker (Kanban #797, 2026-05-12). Optional JSONB array — each element: `{text: str, status: "pending"|"passed"|"failed"|"na", verified_by: str|null, verified_at: ISO datetime|null, notes: str|null}`. Pydantic `AcceptanceCriterion` validates element shape at the API boundary (no DB CHECK — same precedent as `projects.paths`). PATCH semantics: full-replace (no element-merge). `verified_at: datetime|null` requires `model_dump(mode='json')` before JSONB write (Kanban #801 fix). NULL = task filed without structured criteria; `[]` = explicitly cleared. Added by `0014_tasks_acceptance_criteria`. |
| subagent_models | jsonb   | NOT NULL DEFAULT `'[]'`                                             | Append-only audit log of subagent spawns per task (Kanban #887, 2026-05-13). Each element: `{agent: str (min_length=1), model: "opus"\|"sonnet"\|"haiku", at: ISO-8601 UTC datetime}`. Element shape validated by Pydantic `SubagentModelEntry` at the API boundary (no DB CHECK — same precedent as `acceptance_criteria`). Full-replace PATCH semantics: Lead accumulates on its side and sends the full list each PATCH. The column is NOT NULL — the response field is always a list, never null. `at: datetime` requires `model_dump(mode='json')` before JSONB write (same Kanban #801 pattern as `acceptance_criteria.verified_at`). PG 16 metadata-only ADD COLUMN — all pre-existing rows read `[]` without a heap rewrite. Added by `0024_tasks_subagent_models`. |
| blocked_by     | bigint      | NULL, FK `tasks(id)` ON DELETE SET NULL, CHECK `blocked_by IS NULL OR blocked_by <> id` (named `ck_tasks_blocked_by_not_self`) | Single-blocker dependency (Kanban #771, 2026-05-12). PATCH-able post-create (unlike `parent_task_id` / `spawned_from_task_id`). Same-project enforcement at app layer (router POST/PATCH 422 — `"blocked_by {id} belongs to a different project"`). Cycle prevention in router PATCH walks the chain up to depth=10 (`_BLOCKED_BY_MAX_CHAIN_DEPTH`); 422 on cycle or budget exhaust. ON DELETE SET NULL mirrors `spawned_from_task_id` (NOT CASCADE — hard-deleting a blocker must NOT delete the blocked task). Index `ix_tasks_blocked_by` supports the reverse-lookup endpoint `GET /api/tasks/{id}/blocks`. History capture is free via existing `tasks_audit_trg` (`to_jsonb(OLD)`). Added by `0017_tasks_blocked_by`. |
| sort_order     | double precision | NULL                                                            | Within-lane manual sort key (Kanban #772, 2026-05-12). Sparse-float lexicographic ordering — NULL = "use created_at fallback for ordering" within the lane. No CHECK, no FK, no index this slice (sparse-float is constraint-free; lane queries already filter by `process_status` + `status`, both indexed; measured-first index policy). Ordering rule: `ORDER BY sort_order ASC NULLS LAST, created_at ASC`. `POST /api/tasks/{id}/reorder` is the user-facing API and materializes NULL lane-mates on first reorder (floor floats 1.0, 2.0, ...). `PATCH /api/tasks/{id}` accepts direct sort_order set (escape hatch). Cross-row blocker-order constraint at app layer (`routers/tasks.py:_enforce_blocker_order_constraint`): when target.blocked_by walks to B in same lane (process_status=TODO) and both sort_orders non-null, `target.sort_order >= B.sort_order` must hold; violation → 422 (`"task #{T} cannot be ordered before its blocker #{B}"`). Sparse-float collision after ~52 same-position reorders is a known limitation, follow-up filed as #819. History capture is free via existing `tasks_audit_trg`. Added by `0018_tasks_sort_order`. |
| status         | smallint    | NOT NULL DEFAULT 1, CHECK `status IN (0, 1)` (named `ck_tasks_status_valid`) | soft-delete flag |
| created_at     | timestamptz | NOT NULL DEFAULT `now()`                                            |       |
| updated_at     | timestamptz | NOT NULL DEFAULT `now()`                                            | bumped manually in PATCH |
| started_at     | timestamptz | NULL                                                                | router stamps on transition to in_progress |
| completed_at   | timestamptz | NULL                                                                | router stamps on transition to done |

**Indexes:** `ix_tasks_project_id`, `ix_tasks_process_status` (renamed from `ix_tasks_status` 2026-05-08), `ix_tasks_assigned_role`, `ix_tasks_status` (new — on the soft-delete column), `ix_tasks_parent_task_id` (Kanban #238, 2026-05-08), `ix_tasks_next_fire_at_template` (PARTIAL on `(next_fire_at)` WHERE `is_template = TRUE` — scheduler hot-path scan; partial keeps the index small in the dominant non-template subset; Kanban #706, 2026-05-10), `ix_tasks_scheduled_at_pending` (PARTIAL on `(scheduled_at)` WHERE `scheduled_at IS NOT NULL AND process_status = 1 AND status = 1` — one-shot scheduler hot-path scan; NOT unique; Kanban #723, 2026-05-10), `ix_tasks_blocked_by` (b-tree on `blocked_by` — supports reverse-lookup `GET /api/tasks/{id}/blocks`; Kanban #771, 2026-05-12).

**Foreign keys:** `project_id` → `projects(id)` ON DELETE CASCADE; `parent_task_id` → `tasks(id)` ON DELETE CASCADE (self-referential, subtask hierarchy); `spawned_from_task_id` → `tasks(id)` ON DELETE SET NULL (self-referential, recurrence lineage; ORM relationships need `foreign_keys=lambda: [Task.parent_task_id]` selector once the second self-FK landed — Kanban #706); `blocked_by` → `tasks(id)` ON DELETE SET NULL (self-referential, single-blocker dependency; Kanban #771, 2026-05-12).

**Table-level CHECK constraints:** `ck_tasks_template_recurrence_complete`: `is_template = false OR (recurrence_rule IS NOT NULL AND next_fire_at IS NOT NULL)` — DB-level defense-in-depth alongside the Pydantic `_check_template_completeness` model_validator (Kanban #706, 2026-05-10). `ck_tasks_scheduled_xor_template`: `NOT (scheduled_at IS NOT NULL AND is_template = TRUE)` — one-shot vs template fire paths are mutually exclusive; DB-level defense-in-depth alongside Pydantic XOR validator (same-payload) AND router resolved-final XOR (cross-state PATCH) (Kanban #723, 2026-05-10).

### tasks_history
**Purpose:** Audit sink — populated by `tasks_audit_trg` on UPDATE / DELETE. Application code does NOT write here.

| column     | type        | constraints                                | notes |
|------------|-------------|--------------------------------------------|-------|
| id         | bigint      | PK, identity                               |       |
| task_id    | bigint      | NOT NULL                                   | NOT a FK — survives task deletion |
| operation  | char(1)     | NOT NULL, CHECK `operation IN ('U','D')`   |       |
| changed_at | timestamptz | NOT NULL DEFAULT `now()`                   |       |
| snapshot   | jsonb       | NOT NULL                                   | `to_jsonb(OLD)` from trigger |

**Indexes:** `ix_tasks_history_task_id`, `ix_tasks_history_changed_at`

### Audit trigger

```sql
CREATE OR REPLACE FUNCTION tasks_audit_fn() RETURNS trigger AS $$
DECLARE
    op_code char(1);
    row_id  bigint;
BEGIN
    IF (TG_OP = 'UPDATE') THEN
        op_code := 'U';
        row_id  := OLD.id;
    ELSIF (TG_OP = 'DELETE') THEN
        op_code := 'D';
        row_id  := OLD.id;
    ELSE
        RETURN NULL;
    END IF;

    INSERT INTO tasks_history (task_id, operation, changed_at, snapshot)
    VALUES (row_id, op_code, now(), to_jsonb(OLD));

    IF (TG_OP = 'DELETE') THEN
        RETURN OLD;
    ELSE
        RETURN NEW;
    END IF;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER tasks_audit_trg
AFTER UPDATE OR DELETE ON tasks
FOR EACH ROW EXECUTE FUNCTION tasks_audit_fn();
```

### sessions
**Purpose:** One row per Lead bootstrap (or master-agent process) for a project. Per project × per Claude Code instance scope (multi-instance friendly — multiple `status='active'` rows per `project_id` are allowed). Hybrid storage: this row holds metadata + queryability; markdown content lives at `<repo_root>/_sessions/<id>/` (gitignored). **NO audit trigger** — sessions self-audit via `session_compacts`.

| column                          | type           | constraints                                                              | notes |
|---------------------------------|----------------|--------------------------------------------------------------------------|-------|
| id                              | bigint         | PK, identity                                                             |       |
| project_id                      | bigint         | NOT NULL, FK `projects(id)` ON DELETE CASCADE                            |       |
| process_label                   | varchar(64)    | NULL                                                                     | optional human hint |
| status                          | varchar(16)    | NOT NULL DEFAULT `'active'`, CHECK `status IN ('active','compacting','closed')` (named `ck_sessions_status_valid`) | terminal: closed → no further PATCH (router 400) |
| token_budget_per_run            | bigint         | NULL                                                                     | soft budget; NULL = no budget |
| compacted_history_ceiling_tokens| bigint         | NOT NULL DEFAULT 13000                                                   | CTX-4 trigger threshold |
| recent_activity_ceiling_tokens  | bigint         | NOT NULL DEFAULT 15000                                                   | CTX-4 trigger threshold |
| card_detail_ceiling_tokens      | bigint         | NOT NULL DEFAULT 6000                                                    | CTX-3 token-counter bucket (Kanban #722, migration 0009) |
| output_budget_tokens            | bigint         | NOT NULL DEFAULT 4000                                                    | CTX-3 token-counter bucket (Kanban #722, migration 0009) |
| session_root_path               | text           | NOT NULL                                                                 | server-computed: `_sessions/<id>/` (single-COMMIT post-INSERT update via `flush()`) |
| started_at                      | timestamptz    | NOT NULL DEFAULT `now()`                                                 |       |
| closed_at                       | timestamptz    | NULL                                                                     | server-stamped on `status='closed'` |
| created_at                      | timestamptz    | NOT NULL DEFAULT `now()`                                                 |       |
| updated_at                      | timestamptz    | NOT NULL DEFAULT `now()`                                                 |       |

**Indexes:**
- `ix_sessions_project_id` on `(project_id)` — broad listing.
- `ix_sessions_project_id_active` PARTIAL on `(project_id)` WHERE `status='active'` — hot-path "active sessions for project" lookup. **NOT a uniqueness gate** — multiple active rows per `project_id` are allowed (multi-instance support; mirrors post-#694 `is_active` freedom).

**Foreign keys:** `project_id` → `projects(id)` ON DELETE CASCADE.

### session_runs
**Purpose:** One row per task fire / manual run within a session. Cost / token totals roll up here (CTX-3 #718 wires the real values; CTX-1 accepts user-supplied totals with no validation). FK to `tasks` is SET NULL — preserves the run audit row when a task is later hard-deleted.

| column               | type           | constraints                                                              | notes |
|----------------------|----------------|--------------------------------------------------------------------------|-------|
| id                   | bigint         | PK, identity                                                             |       |
| session_id           | bigint         | NOT NULL, FK `sessions(id)` ON DELETE CASCADE                            |       |
| task_id              | bigint         | NULL, FK `tasks(id)` ON DELETE SET NULL                                  | preserves audit on hard-delete of task |
| status               | varchar(16)    | NOT NULL DEFAULT `'running'`, CHECK `status IN ('running','done','error','timeout')` (named `ck_session_runs_status_valid`) | terminal states auto-stamp `finished_at` |
| started_at           | timestamptz    | NOT NULL DEFAULT `now()`                                                 |       |
| finished_at          | timestamptz    | NULL                                                                     | server-stamped on transition to terminal status |
| total_input_tokens   | bigint         | NOT NULL DEFAULT 0                                                       | CTX-3 will replace with server-authoritative |
| total_output_tokens  | bigint         | NOT NULL DEFAULT 0                                                       |       |
| total_context_chars  | bigint         | NOT NULL DEFAULT 0                                                       |       |
| total_cost_usd       | numeric(10,4)  | NOT NULL DEFAULT 0                                                       | CTX-1 accepts client-supplied; CTX-3 = server-authoritative |
| budget_warning       | boolean        | NOT NULL DEFAULT false                                                   | set when token total exceeded `session.token_budget_per_run` |
| card_log_path        | text           | NULL                                                                     | `_sessions/<sid>/cards/<task_id>.md` when task_id given |
| created_at           | timestamptz    | NOT NULL DEFAULT `now()`                                                 |       |
| updated_at           | timestamptz    | NOT NULL DEFAULT `now()`                                                 |       |

**Indexes:** `ix_session_runs_session_id`, `ix_session_runs_task_id`.
**Foreign keys:** `session_id` → `sessions(id)` ON DELETE CASCADE; `task_id` → `tasks(id)` ON DELETE SET NULL.

### session_compacts
**Purpose:** One row per compact event within a session. CTX-4 #719 wires the runner; CTX-1 ships the schema + read endpoints only.

| column           | type           | constraints                                                              | notes |
|------------------|----------------|--------------------------------------------------------------------------|-------|
| id               | bigint         | PK, identity                                                             |       |
| session_id       | bigint         | NOT NULL, FK `sessions(id)` ON DELETE CASCADE                            |       |
| trigger_kind     | varchar(16)    | NOT NULL, CHECK `trigger_kind IN ('size','manual','run_count')` (named `ck_session_compacts_trigger_valid`) |       |
| archive_path     | text           | NOT NULL                                                                 | `_sessions/<sid>/archive/compact_NNN.md` |
| before_tokens    | bigint         | NOT NULL                                                                 |       |
| after_tokens     | bigint         | NOT NULL                                                                 |       |
| compact_model    | varchar(64)    | NOT NULL                                                                 | e.g., `claude-haiku-4-5-20251001` |
| compact_cost_usd | numeric(10,4)  | NOT NULL DEFAULT 0                                                       |       |
| compacted_at     | timestamptz    | NOT NULL DEFAULT `now()`                                                 |       |

**Indexes:** `ix_session_compacts_session_id`.
**Foreign keys:** `session_id` → `sessions(id)` ON DELETE CASCADE.

**No audit trigger** on `sessions`, `session_runs`, or `session_compacts` — distinct from `tasks` / `tasks_history`. Sessions self-audit via the `session_compacts` archive history (each compact event archives the prior `session.md` to `_sessions/<id>/archive/compact_NNN.md`). The `tasks_audit_trg` pattern is `tasks`-only.

<!-- No tables yet. First table goes above this line. -->

## Pending migrations (generated, not yet applied)

_(none)_

## Migrations log

<!--
Format: YYYY-MM-DD HH:MM — <migration filename> — applied by <who> in commit <sha>

- 2026-05-04 12:30 — 2026_05_04_1230_create_users.py — devops, commit abcd123
-->

- 2026-05-08 09:46 — 2026_05_08_0300_soft_delete_and_lead.py — dev-devops, container apply
- 2026-05-08 16:00 — 2026_05_08_1600_tasks_parent_task_id.py — dev-devops, container apply (Kanban #238)
- 2026-05-09 09:00 — 2026_05_09_0900_rename_lead_to_team.py — dev-devops, container apply (Phase 2.5b1 — pure DDL rename projects.lead → projects.team)
- 2026-05-09 10:00 — 2026_05_09_1000_run_mode_and_consent.py — dev-devops, container apply (Kanban #482 — Step 2 prep schema seam: tasks.run_mode + projects.auto_run_consent_at; round-trip up→down→up verified; 464/464 tasks DEFAULT-backfilled to `manual`; ORM/Pydantic/router wire-up deferred to #483-B)
- 2026-05-10 00:50 — 2026_05_10_0050_drop_active_one.py — dev-devops, container apply (Kanban #694 Phase 2 — drop `ux_projects_active_one` partial unique index after session-scoped active project shift; round-trip up→down→up verified; agent-teams id=1 byte-identical pre/post; remaining indexes `ux_projects_name_active`, `ix_projects_status`)
- 2026-05-10 11:00 — 2026_05_10_1100_task_kind_and_recurrence.py — dev-devops, container apply (Kanban #706 — V3+ T1 schema foundation: `tasks.task_kind` + 5 recurrence template fields + 2 CHECKs (`ck_tasks_task_kind_valid`, `ck_tasks_template_recurrence_complete`) + 1 partial index (`ix_tasks_next_fire_at_template`) + 1 self-FK (`spawned_from_task_id` ON DELETE SET NULL); round-trip up→down→up verified on scratch DB before live apply; 53/53 tasks backfilled metadata-only via PG 16 ADD COLUMN-with-DEFAULT; agent-teams id=1 byte-identical pre/post; container rebuild required first because `croniter>=2.0,<7.0` was added to `pyproject.toml`)
- 2026-05-10 13:00 — 2026_05_10_1300_sessions_and_runs.py — dev-devops, container apply (Kanban #716 — CTX-1 hybrid storage foundation: `sessions` (12 cols + 1 CHECK `ck_sessions_status_valid` + 2 indexes incl. partial `ix_sessions_project_id_active WHERE status='active'` — **NOT UNIQUE**, multi-instance support) + `session_runs` (14 cols + 1 CHECK `ck_session_runs_status_valid` + 2 indexes) + `session_compacts` (9 cols + 1 CHECK `ck_session_compacts_trigger_valid` + 1 index). FKs: sessions→projects CASCADE; session_runs→sessions CASCADE, session_runs→tasks SET NULL; session_compacts→sessions CASCADE. **NO audit trigger** on the 3 new tables — sessions self-audit via `session_compacts` archive history. Round-trip up→down→up verified on scratch DB before live apply; agent-teams id=1 byte-identical pre/post; tasks count 62 unchanged. NO container rebuild needed (no new pyproject deps).)
- 2026-05-10 15:00 — 2026_05_10_1500_session_ceilings_extension.py — dev-devops, container apply (Kanban #722 — CTX-1 audit follow-up: adds `sessions.card_detail_ceiling_tokens BIGINT NN DEFAULT 6000` + `sessions.output_budget_tokens BIGINT NN DEFAULT 4000` to close 4-bucket-doc-vs-2-bucket-schema gap. Schema-additive; `server_default` backfilled all 3 pre-existing rows to 6000/4000. Round-trip up→down→up verified on scratch DB before live apply. Pydantic schemas extended with bounds `ge=1, le=1_000_000` (M2 — operator-typo guard); negative-zero test added (M3). NO container rebuild. Downstream: CTX-3 #718 token counter reads the 4 ceiling columns.)
- 2026-05-12 21:40 — 2026_05_12_2137_tasks_interaction_kind.py — dev-devops, container apply (Kanban #830/#831 — headless auto-run schema: `tasks.interaction_kind VARCHAR(16) NOT NULL DEFAULT 'work'` + `ck_tasks_interaction_kind_valid` + `tasks.question_payload JSONB NULL` + `tasks.resume_context JSONB NULL`; all existing rows backfilled interaction_kind='work' via PG 16 metadata-only ADD COLUMN; 0 rows with interaction_kind != 'work' confirmed post-apply; API 500 restored to 200 after apply)
- 2026-05-10 17:00 — 2026_05_10_1700_tasks_scheduled_at.py — dev-devops, container apply (Kanban #723 — V3+ T1 audit follow-up: adds `tasks.scheduled_at TIMESTAMPTZ NULL` (one-shot scheduler fire path) + partial index `ix_tasks_scheduled_at_pending ON tasks(scheduled_at) WHERE scheduled_at IS NOT NULL AND process_status = 1 AND status = 1` (NOT unique) + table-level CHECK `ck_tasks_scheduled_xor_template`: `NOT (scheduled_at IS NOT NULL AND is_template = TRUE)`. Closes the one-shot scheduling gap missed by T1 #706 (which only modeled the recurring/template path). Schema-additive; PG 16 `ADD COLUMN` on nullable column = metadata-only, all 67 existing rows backfilled NULL. Round-trip up→down→up verified on scratch DB before live apply. Defense-in-depth XOR pairing: Pydantic `_check_scheduled_xor_template` on TaskCreate+TaskUpdate (same-payload, 422) + router resolved-final check on PATCH (cross-state direction-A and direction-B, 422) + DB CHECK (raw-SQL backstop, 400). All three layers reuse a single source-text-locked `_DETAIL_SCHEDULED_XOR_TEMPLATE` constant. NO container rebuild. Downstream: T2 #707 scheduler scans both fire paths (templates via `next_fire_at`, one-shots via `scheduled_at`).)
- 2026-05-13 11:32 — 2026_05_13_1132_projects_sources.py — dev-devops, container apply (Kanban #778 — migration lane: adds `projects.sources jsonb NULL DEFAULT '[]'::jsonb` + CHECK `ck_projects_sources_length` (`jsonb_array_length(sources) <= 20`). PG 16 metadata-only ADD COLUMN — every existing row reads `[]` post-apply. Round-trip up→down→up verified on the live container DB before commit-of-record. CHECK-fail manual verification deferred to the dev-backend follow-up pytest under `Session.rollback()` (PreToolUse hook `block-raw-sql-dml.ps1` correctly blocked the subagent's BEGIN/UPDATE/ROLLBACK probe — read-only `pg_constraint` confirms the CHECK is live). ORM/Pydantic/router wire-up follows in the dev-backend lane of the same Kanban task.)
- 2026-05-13 14:30 — 2026_05_13_1430_projects_team_general.py — dev-backend, container apply (Kanban #844 — adds `'general'` to `ck_projects_team_valid` CHECK via drop+recreate idiom; mirrors `0004_rename_lead_to_team` pattern. Atomic landing across 6 sites: alembic + `api/src/constants.py` ProjectTeam.GENERAL + `api/src/schemas/project.py` TeamCode Literal (lockstep guard at import-time catches drift) + ORM CheckConstraint reads from `ProjectTeam.ALL` dynamically + `project_scaffold.py` TEAM_ROSTERS[GENERAL]=('general',) + `zero_config_scaffold.py` + 1-line `web/lib/constants.ts` FE mirror. Round-trip up→down→up verified on live container DB; agent-teams id=1 / NewsAnalyzer id=567 / Writing id=568 byte-identical team values pre/post. Downgrade caveat: rows with `team='general'` block CHECK recreate — operator cleans via API, never raw SQL DML. POST team='general' → 201 + scaffolds `general/` role folder; POST team='lolwut' → 422 Pydantic Literal lists all 3 values.)
- 2026-05-13 14:58 — 2026_05_13_1458_tasks_task_kind_default_ai.py — dev-backend, container apply (Kanban #858 — `ALTER COLUMN task_kind SET DEFAULT 'ai'` (was `'human'`). Non-destructive — only affects new INSERTs; existing rows preserve their stored value (live counts pre/post: ai 45→46, human 175→175). Round-trip up→down→up verified on live DB. New service `coerce_task_kind_for_interaction` in `services/task_kind.py` silently sets `task_kind='human' + run_mode='manual'` when resolved `interaction_kind IN ('question','decision')` — Option A atomic coerce, posture-matched to the column-default flip. 7 new pytest cases in `test_task_kind_default_and_coerce.py`; full suite 499 passed / 1 pre-existing flake (test_777_edge_soft_delete_recreate_isolates_working_path; same flake seen in #844/#854 sessions).)
- 2026-05-13 17:00 — 2026_05_13_1700_tasks_cancelled_and_reason.py — dev-backend, container apply (Kanban #854 — drops+recreates `ck_tasks_process_status_valid` to include `6` (CANCELLED); adds `tasks.status_change_reason TEXT NULL`. PG 16 metadata-only ADD COLUMN on nullable text. Atomic landing: migration + constants.TaskStatus.CANCELLED + ORM mapped column + schemas + router (`GET /api/tasks` default-excludes ps=6, `?include_cancelled=true` opt-in) + `GET /api/projects/stats` emits `counts["6"]` and EXCLUDES cancelled from `last_activity_at` (Option A — parity with soft-delete dead-end-work exclusion semantics) + 1-line `web/lib/constants.ts` FE mirror. Round-trip up→down→up verified on live container DB. Audit trigger `tasks_audit_trg` snapshots `status_change_reason` automatically via `to_jsonb(OLD)` — locked by new test `test_patch_cancel_records_reason_in_tasks_history`. Downgrade caveat: rows with `process_status=6` block CHECK recreate — operator PATCHes back to 1 or 5 via API first; never raw SQL DML. Pytest 1 pre-existing flake `test_777_edge_soft_delete_recreate_isolates_working_path` (passes in isolation; affects #844 + #854 sessions; flake-investigation follow-up filed.)
- 2026-05-13 — 2026_05_14_0001_tasks_subagent_models.py — dev-devops, container apply (Kanban #887 — `ADD COLUMN subagent_models JSONB NOT NULL DEFAULT '[]'` on `tasks`. PG 16 metadata-only op. All existing rows default to `[]`; NULL count = 0 verified post-apply. No heap rewrite. Shape validated by `SubagentModelEntry` Pydantic model at API layer — no DB-level CHECK on element shape.)
