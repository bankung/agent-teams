# Architectural & process decisions — dev team

> **Lead is the only writer of this file.** Subagents propose updates in their final report — Lead reviews, may ask the user, then writes the entry.
>
> **Scope:** decisions about the **dev team methodology / orchestration system itself** — Tier-1 smoke flow, Tier-2 release wrap-up, lifecycle steps, agent prompts, Bucket architecture. Applies to **every** `team='dev'` project, not to any one project's data model or endpoints.
>
> Per-project decisions (a project's data model, endpoints, UI, migrations, deps) live in that project's `context/projects/<project>/shared/decisions.md`. When a project-specific incident produces a methodology lesson, the incident decision goes in the project file and the methodology decision goes here, cross-linked.
>
> Format: append-only log. Newest entry at the top. Each entry has a date, scope, who proposed it, and the reasoning.
>
> **Note on Kanban references:** entries below reference Kanban ids from the `agent-teams` project (the dogfood project where this methodology was first validated). When a future dev project under this methodology cites these entries, the referenced incident lives in `agent-teams/shared/decisions.md` — not their own project's log.

<!--
Template for a new entry:

## YYYY-MM-DD — <short title>
**Scope:** team-playbook | agent-prompts | bucket-architecture | lifecycle | other-cross-project
**Proposed by:** <role> (or `lead` / `user`)
**Decision:** <what we decided to do>
**Reasoning:** <why — constraints, tradeoffs, alternatives considered>
**Implications:** <what changes downstream>
-->

## 2026-05-30 — Agent tier-audit artifact relocated here from bin/tier-presets/ — Kanban #1685
**Scope:** team-playbook / methodology / agent tiering
**Proposed by:** lead (code-minimization / hygiene combo #1685 + #1657)

The one-time agent tier-audit (Kanban #1360, generated 2026-05-21) lived as `bin/tier-presets/AUDIT.md` — a 37-row table mapping every `.claude/agents/*.md` to its MAX vs L2 (Pro-pilot) tier with downgrade-risk notes. That's methodology content (which roles run on which model tier + why), not a build artifact, so it didn't belong under `bin/`. Summary preserved here; the original file was removed.

**Tiering decision captured (from the audit):**
- **MAX preset** = operator's full baseline (no downgrades).
- **L2 (Pro pilot) preset** = routine roles → Sonnet; `dev-sr-*` + strategists (`bi-analyst`, `seo-strategist`, `sem-campaign-lead`) → Opus; `dev-documentor` / `general-researcher` → Haiku.
- Roles already at L2 (no change): dev-backend/frontend/devops/tester/reviewer/spec-reviewer/security-reviewer/analyst, project-auditor, content-editor/hook-doctor/seo-optimizer/veracity-checker, thai-proofreader (MAX), etc.
- L2 downgrades flagged with risk notes: `general` (implicit-Opus → Sonnet), `content-writer` (Opus → Sonnet — revert for high-stakes content), `thai-proofreader` (Sonnet → Haiku — spot-check the first batch).
- **Canonical applied record = the diffs under `bin/tier-presets/l2/`** (the actual frontmatter changes). This entry is the rationale; that directory is the source of truth for what L2 alters.

## 2026-05-29 — Team/agent onboarding runbook + netops as the validating instance — Kanban #1643
**Scope:** team-playbook / methodology / platform-onboarding
**Proposed by:** lead (netops diagnosis-team PoC)
**Decision:** Capture the post-#1620 add-a-team / add-an-agent blast radius + residual friction as a reusable runbook: [`context/teams/dev/team-onboarding-runbook.md`](team-onboarding-runbook.md). netops (read-only network diagnosis) was onboarded as the live regression test: `ProjectTeam.NETOPS` + a roster entry + `.claude/teams/netops.md` + agents; a netops project (id=660) was created via `POST /api/projects` with **zero migration files added**, validating #1620's single-source team enum end-to-end.
**Reasoning:** #1620 made "add a team" a `constants.py` edit (no migration), but the *full* cost wasn't documented. Onboarding netops surfaced four residual frictions worth recording so the next onboarding doesn't trip: (1) **agent loading is operator-gated** — new `.claude/agents/*.md` are humans-only AND load only at session start, so adding an agent = code edit + humans-only file move + restart, never a single live commit; (2) **`TEAM_ROSTERS` ↔ `.claude/agents/` coupling is load-bearing** — a roster entry without its `.md` degrades scaffold manifest resolution (`zero_config_scaffold._resolve_manifest` logs the missing file to the scaffold report's errors rather than raising; those roles scaffold with no agent definition), so the constants edit and the file move MUST land together (this is why #1643 shipped the netops-triage/l2l3 roster expansion as an operator batch, not a pre-commit); (3) **no `TaskRole` band for netops** — spawn-by-name only, not Kanban-assignable; (4) **no netops node in the langgraph graph** — netops is interactive-only, so the scheduled sweep is `run_mode=manual` (fires interactive TODOs), not `auto_pickup`. Per the keep-original-spec decision (#1643), a dedicated `netops-triage` agent was added alongside `netops-l2l3` (drafted to `_scratch/netops-build/` for the operator move).
**Implications:** Future team/agent onboarding reads the runbook first. The recurrence sweep (template task #1648 under the netops project) demonstrates the scheduled-wiring path but stays `run_mode=manual` until a Phase-2 langgraph netops node lands. AC#5 (agents invokable post-restart) + AC#6 (on-demand PoC from real Zabbix data) are operator-gated / deferred follow-ups. Pure docs/methodology + DB-artifact change — no app code, no migration, no pytest delta.

## 2026-05-29 — Doc status-marker convention (production / in active development / roadmap) — Kanban #1642
**Scope:** team-playbook / docs-standards / lifecycle
**Proposed by:** lead (follow-up to the #1641 honest-docs sweep)
**Decision:** Codify a lightweight status-marker convention for user-facing docs. Full text in [`context/teams/dev/doc-status-convention.md`](doc-status-convention.md). In short:
- **Three states, canonical phrases (case-insensitive, verbatim):** `production`, `in active development`, `roadmap`. Synonyms ("under development", "coming soon", "WIP", "beta", "experimental") are banned because they defeat the grep that finds every occurrence.
- **Marker form:** `**Status: <State>.**` as the first line under a capability heading, with an optional one-clause qualifier. Inline prose uses the lowercase phrase; both contain the canonical string.
- **Where:** every *non-production* claim MUST be marked wherever it appears; capability-catalog docs (USAGE-POWER) mark every entry for a scannable column; narrative docs (README) keep prose and only ensure the canonical phrase is used (no badge clutter).
- **Single source of truth — ADOPTED, project-scoped:** [`context/projects/agent-teams/shared/component-status.md`](../../projects/agent-teams/shared/component-status.md) is the canonical maturity registry. Other dev projects create their own `shared/component-status.md` on first need.
**Reasoning:** #1641 found the headless-engine "in active development" caveat had drifted into FOUR docs in four different wordings, with no marker — easy to overclaim a roadmap feature as live, hard to find. The fix is a *single canonical, greppable phrase* + a *registry as the reconciliation target*, so a status change is "update one row, then grep-and-fan-out" instead of "edit one doc and hope the rest follow." **Honest non-decisions (rejected by design):** (1) **emoji badges** — declined; text-only keeps a professional tone for the public job-application audience and removes rendering ambiguity. (2) **A "source of truth" that auto-syncs doc prose** — impossible in static Markdown (no transclusion); the registry is the canonical *answer* + maintainer *checklist*, not an auto-sync engine, and the docs still carry their own inline markers. (3) **Badging every README paragraph** — declined; README's narrative prose already carries honest inline framing, and `Status:` lines on marketing bullets would hurt readability. The registry is project-scoped (it lists *agent-teams's* components) per Q2, while the convention is team methodology (applies to every dev project's docs).
**Implications:** When a component's maturity changes, the maintenance sweep is: edit the registry row first → `grep -ri "<canonical phrase>"` the docs → update every match in the same change → final grep for stale phrases. dev-documentor (or whoever edits docs) reads the convention before a status edit. Applied in this task to README, USAGE-POWER, QUICKSTART, CLAUDE-CODE-START. Pure docs/methodology change — no code, no schema, no pytest delta.

## 2026-05-28 — Validate with full test FILES, not scoped selectors (regression-detection floor) — Kanban #1599
**Scope:** lifecycle / methodology
**Proposed by:** lead (after #1599 full-suite triage)
**Decision:** When a task touches a migration, model, router, or any shared surface, the verifying pytest run MUST execute the affected test FILE(S) in full (ideally the full suite for migration/schema changes) — NOT a narrow node-id selector scoped to the new behavior. A scoped selector only proves the new path; it cannot catch a regression in a *sibling* test that exercises the same surface differently.
**Reasoning:** This gap has now caused TWO shipped regressions on agent-teams:
- #1618 broke `test_routes_smoke.py::test_777_edge_scaffold_uses_repo_root_not_working_path` — the scoped run validated the new scaffold-skip behavior but not the existing test that asserted the old behavior.
- #1620's migration 0051 (no-op downgrade) broke `test_tool_calls.py::test_migration_downgrade_then_upgrade_leaves_clean_state` — the down→up roundtrip test was never in the scoped validation set, so the broken downgrade chain shipped and only surfaced in #1599's full-suite run.
Both were cheap to fix once found and would have been caught by running the full file/suite at task-verify time. Migration/schema changes specifically must run the full suite (the migration-roundtrip test lives in an unrelated file).
**Implications:**
- Spawn briefs for migration/schema/router work should require a full-file (or full-suite) pytest run in the verification AC, not a node-id selector.
- The api full suite is a reliable deterministic signal again (see agent-teams `shared/decisions.md` #1599 — concurrent-invocation filelock guard); "green" = a known fixed count, so a full run is a trustworthy gate.

## 2026-05-28 — Add-team / add-agent edit floor + TaskRole coupling — Kanban #1620
**Scope:** team-playbook / agent-roster / methodology
**Proposed by:** lead (agent-teams #1620 team-SSOT refactor, after 5 design-review rounds)
**Decision:** Canonical cost of extending the roster after the #1620 single-source refactor (team enum is now app-validated against `api/src/constants.py` `ProjectTeam.ALL` + `TEAM_ROSTERS`; no CHECK constraint, no per-team migration):
- **Add a team:** edit `constants.py` (ProjectTeam value + TEAM_ROSTERS tuple) + author `.claude/teams/<team>.md` playbook + author `.claude/agents/<role>.md` for any NEW roles. NO migration; ORM/FE/ps1 all derive from constants + `GET /api/teams`.
- **Add an agent to an existing team:** drop `.claude/agents/<role>.md` + add the role name to that team's TEAM_ROSTERS tuple → spawnable + scaffolded. For FIRST-CLASS Kanban-assignability (`tasks.assigned_role` routing, Lead spawn→code mapping, `config.enabled_roles` gating) ALSO add a named `TaskRole` code in the team's 10-code band.
- **Ceiling:** `TaskRole.RANGE_MAX = 50` + the 10-code-per-team partition (dev 1-10, novel 11-20, seo 21-30, sem 31-40, data-analytics 41-50). A 6th non-dev team or an over-full roster needs a RANGE_MAX bump + partition rethink. content/general have no TaskRole band today.
**Reasoning:** #1620 made the common case (1-2 edits + drop .md) true, but TaskRole integer codes remain a real coupling for Kanban-first-class agents (surfaced in design-review rounds 3-4). Documenting the floor prevents a future "added the .md but the agent isn't assignable / pushed past RANGE_MAX" surprise.
**Implications:** Optional hardening — a startup sanity check that every TEAM_ROSTERS role maps to a named TaskRole code would turn silent drift into a loud warning. The dedicated-vs-borrowed roster distinction (TEAM_ROSTERS = dedicated agents that own a role-state folder; cross-team-reuse agents stay ad-hoc) is the rule that keeps the convention-derived scaffold manifest correct.

## 2026-05-14 — Claude Code Desktop worktree-per-session is hardcoded; cross-session shared/* invisibility is the friction
**Scope:** team-playbook / multi-session-safety / lifecycle
**Proposed by:** lead (NewsAnalyzer session 2026-05-14, verified via `claude-code-guide` agent vs docs.claude.com)
**Status:** finding + remediation guide — no code change to agent-teams or its dependents.

**Finding:**
- **Claude Code Desktop hardcodes per-session worktree creation.** Every Desktop session opens a fresh worktree at `.claude/worktrees/<adjective>-<noun>-<6hex>/` and a fresh branch `claude/<adjective>-<noun>-<6hex>` from `main`. There is NO `settings.json` toggle, environment variable, or UI checkbox to disable. Confirmed against [code.claude.com/docs/en/desktop.md](https://code.claude.com/docs/en/desktop.md#work-in-parallel-with-sessions) and [code.claude.com/docs/en/settings.md](https://code.claude.com/docs/en/settings.md#worktree-settings) (retrieved 2026-05-14). The only configurables are: worktree LOCATION, branch PREFIX, auto-archive-after-PR-merge — none of which turn the feature off.
- **Claude Code CLI** treats worktree as **opt-in** via `claude --worktree <name>`. Without the flag, the session runs directly on the current branch.
- The interaction with our Lead protocol: `context/projects/<p>/shared/*` writes from session A live on `claude/<A-slug>` branch — invisible to session B that branches from `main` BEFORE A's branch was merged.

**Concrete incident (2026-05-14 NewsAnalyzer session):** User said "มาต่อจากที่ดูกันเมื่อกี้" expecting decisions.md continuity. Prior session (`claude/relaxed-gould-c89537`) had committed `177f467 docs(NewsAnalyzer): lock engine + scope + cost rules in shared/decisions.md` ~30 min earlier, but the branch was never merged. Fresh session (`claude/stoic-archimedes-66ea3b`) branched from `main` saw an empty `decisions.md`. Recovery cost ~5 turns: `git merge --ff-only` + fast-forward of the active worktree. Memory entry: [feedback_no_branch_per_task.md](~/.claude/projects/.../feedback_no_branch_per_task.md) extended with mechanism details + cleanup procedure.

**Why this matters for the dev team specifically:**
- The session-scoped active project protocol (this file's 2026-05-09 'Session-scoped active project' entry) assumed `shared/*` files are durably accessible across sessions. Worktree-per-session breaks that assumption silently — the file IS durable, it's just on a branch the next session can't see.
- The Lead-as-sole-writer convention for `shared/*` is undermined: technically Lead writes, but the WRITE is invisible to the next Lead unless the worktree branch lands on main.
- Phase 3's `X-Project-Id` header (lock entry 2026-05-09 cross-ref to #695) gates against DB-side mis-binding under compaction, but cannot help with filesystem-side state (decisions.md, shared/docs/, etc.).

**Remediation options (Lead-side, no harness change required):**

1. **Always ff-push worktree branch to main BEFORE session close.** Pattern: `git push origin <worktree-branch>:main` (works if branch is a clean ancestor of main) OR `git checkout main && git merge --ff-only <worktree-branch>`. This is the safest workflow for solo users on Desktop. Codify in the session-close routine.

2. **Encourage CLI for solo work where Desktop UI features aren't needed.** Direct-on-main eliminates the cross-session invisibility entirely. User's call per session — Lead surfaces the choice rather than picking.

3. **Worktree cleanup on Windows requires post-session manual step.** The harness holds file handles on `.claude/worktrees/<slug>/` while a Claude Code instance is running. Mid-session `rm -rf` returns "Device or resource busy". Document the post-close PowerShell sequence in any spawn brief that creates orphan worktrees.

4. **Don't ship Lead-protocol changes that depend on filesystem-side cross-session durability without a harness-side guarantee that worktrees merge.** Current Lead-bootstrap reads `shared/decisions.md` — fine as long as point (1) is followed. Anything fancier (e.g., "next Lead picks up halt_reason from previous Lead's shared/* file") would compound the failure mode.

**Reasoning:**
- The friction is real but the fix is procedural (commit + ff-merge before close), not architectural. Pivoting agent-teams to avoid filesystem-side cross-session state would be over-engineering for a solo user.
- The Lead-protocol documents (CLAUDE.md, .claude/teams/dev.md) already assume `shared/*` is the source of truth between sessions; surfacing the worktree caveat closes the implicit-assumption gap.
- Recording in team-methodology rather than agent-teams's project-shared because the finding applies to every dev project orchestrated via agent-teams (NewsAnalyzer, Writing, agent-teams dogfood, future projects) — Q2 of the Q0-Q2 framework places this firmly in team methodology.

**Implications:**
- Next agent-teams session that hits a "shared/* write from prior session not visible" symptom should (a) check `git branch -a --contains <expected-commit>` to locate the orphan branch, (b) `git merge --ff-only <branch>` if clean fast-forward possible, (c) update memory if a new failure mode surfaces.
- Lead's session-close routine SHOULD include: `git push origin <branch>` (preserve work) + offer to ff-merge to main if applicable + warn about cleanup.
- A user-facing item that may warrant filing as a Kanban task: "Session close hook — auto-ff-merge to main on clean exit." Out of scope for this entry; deferred to user.
- Memory record at [`feedback_no_branch_per_task.md`](~/.claude/projects/.../feedback_no_branch_per_task.md) carries the cross-session-applicable shape; this entry carries the agent-teams-project narrative + Q0-Q2 placement rationale.

**Cross-references:**
- 2026-05-09 'Session-scoped active project' entry above — multi-terminal binding gap, the spiritual sibling to this one (DB-side; this one is filesystem-side).
- Memory: `feedback_no_branch_per_task.md` (mechanism + cleanup procedure detail).
- Recovery example: `agent-teams` git log 2026-05-14 — `git merge --ff-only claude/relaxed-gould-c89537` on local main + `git push -u origin claude/stoic-archimedes-66ea3b` (feature branch push pattern).

## 2026-05-12 — Decay policy locked: 3 classes (perishable / review-on-touch / evergreen) + 4 demote triggers + frontmatter contract
**Scope:** bucket-architecture / lifecycle (governs `context/projects/<p>/shared/docs/` + `context/teams/dev/` writes — does NOT apply to standards, decisions logs, or `_scratch/`)
**Proposed by:** user (clarification request after promote/demote discussion on dev-researcher dnd-kit doc — Kanban #812)
**Decision:** Every doc Lead promotes to `shared/docs/` or writes to `context/teams/dev/` (other than this policy, lessons, and decisions logs) MUST declare `decay_class` in frontmatter — one of `perishable` (with `decay_after` ISO date + `decay_trigger` event), `review-on-touch` (with `review_when` description), or `evergreen` (no TTL — supersession-only). Demote/prune fires per 4 named triggers: (1) generality decay, (2) code-authoritative now, (3) stale snapshot, (4) scope retired. Full text in [`context/teams/dev/decay-policy.md`](decay-policy.md).

**Reasoning:** `_scratch/` vs `shared/docs/` is a binary today — but some "keepers" are inherently perishable (lib API snapshots, security advisories) while others are evergreen (decisions). Without a class system, all docs age into the same "is this still current?" doubt that silently erodes trust in the whole tree. Tagging at write time + on-demand check at read time avoids the trap of (a) tagging everything as decay-able (signal collapses) and (b) building infra to scan staleness preemptively (latency tax for marginal value). Researcher's dnd-kit doc was the first concrete case that needed the policy.

**Key non-decisions (rejected by design):**
- **TTL on evergreen docs (decisions, lessons):** age = feature not bug; older entries are more proven.
- **Track "not read in Y days":** filesystem mtime ≠ read time; instrumenting reads = new infra for marginal value. Neglect filters naturally; let trigger #1 (generality decay) clean up when the doc next surfaces.
- **Session-bootstrap auto-scan:** adds latency to every session; check at the read point instead.

**Implications:**
- Lead writes/promotes to `shared/docs/` must include the frontmatter going forward — no retroactive sweep required (existing docs are grandfathered as `review-on-touch` until next edit).
- `dev-documentor` and `dev-researcher` agent prompts should reference this policy when their output is candidate for promotion. (Soft enforce — Lead applies the frontmatter at promote time; agents don't need to write it themselves.)
- An `/audit-decay` slash command is logged as a future option but NOT built; the on-demand check at embed time is the primary enforcement.
- This is the first cross-cutting governance rule that applies to **Lead-written docs** (as opposed to standards or code). Pattern may grow if other write-zones need similar declarations.

## 2026-05-09 — Session-scoped active project: bootstrap asks user, supports parallel terminals
**Scope:** team-playbook / bootstrap / multi-project safety
**Proposed by:** user (after audit-2026-05-09 surfaced multi-terminal Gap A — "two terminals see the same global active row")
**Decision:** Shift the "active project" abstraction from **global DB state** to **per-session user-bound state**. The new bootstrap protocol, replacing the prior `GET /api/projects/active` first-call:

1. Lead's **first action** of every new session is to ask the user: "Which project are we working on?" — pinned as a literal question. Lead waits for a reply before any other tool call (no shared/* read, no API call, no spawn). If the user's first message already names a project explicitly (e.g. "ทำ task #406 ใน agent-teams"), Lead may skip the question and proceed to step 2.
2. Lead resolves the named project via `GET /api/projects/by-name/<name>` (existing endpoint).
3. Lead announces "Session bound to <name> (team=<team>, id=<id>)" — this is the session-bound project for every subsequent API call, file read/write, and subagent spawn brief in this session.
4. Each Claude Code session binds independently — **multiple terminals may run in parallel against different projects**. The DB's `is_active` flag is no longer load-bearing for "which project is this session about"; the session-bound name (held in Lead's working memory + announced to user) is.

**Phase 1 (immediate):** CLAUDE.md `## Bootstrap` section rewritten to enforce the ask-and-bind flow. NO code change, NO schema change, NO test change — the new behavior is purely a Lead protocol shift, and is backward-compatible with the existing `is_active`-based UI / API endpoints (those continue to work; they just stop being authoritative for session-scoped active).

**Phase 2 (filed as separate Kanban task):** Drop `ux_projects_active_one` partial unique index (migration 0006); redefine `is_active` semantics from "the one focus" → "live (vs archived)"; update tests that assert uniqueness; surface standards proposals (`postgresql/soft-delete.md`, `fastapi/atomic-mutations.md`, `fastapi/routing.md`, `sqlalchemy/migrations.md` all reference the partial-unique pattern as a worked example — the example stays valid as a PG technique, but the agent-teams instance changes from "exemplar" to "former exemplar"). UI follow-up TBD.

**Reasoning:** The `ux_projects_active_one` constraint was a sensible default when the system was a single-user, single-task tool — "the user's current focus" mapped 1:1 to "the one row with is_active=true." It scales poorly to (a) multi-terminal parallel work, (b) auto-pickup queue runners (Step 2 Mode A2 — see 2026-05-09 'Kanban-driven AI' entry above), and (c) the dogfood-adjacent "I'm reviewing this project AND working on that one" workflow. Audit-2026-05-09 surfaced this as Gap A: two terminals call `/api/projects/active`, both see the same row, Lead in the wrong terminal might write to the wrong project. The user's lock — "ask user at session start, require reply before working" — is the cheapest possible fix: it removes the silent-coupling between terminals without touching the DB. **The friction of typing a name IS the gate** (same UX premise as the typed-acknowledgment consent endpoint locked in #483).

The phased rollout (CLAUDE.md text → Phase 2 schema/tests/standards) was chosen because Phase 1 alone delivers the user-facing benefit (parallel terminals work cleanly via Lead asking user) without touching any test, schema, or API contract. Phase 2 is cleanup to make the DB shape match the new behavior.

**Implications:**
- Bootstrap section of CLAUDE.md changes shape — first action is a question to user, not a `curl /api/projects/active`. Subagent spawn briefs continue to specify the project explicitly (no change).
- New sessions follow the new protocol; old in-flight sessions stay bound to whatever they bootstrapped against. CLAUDE.md is loaded fresh per session, so the rollout is automatic.
- `GET /api/projects/active` endpoint continues to exist; semantics unchanged. Future: when Phase 2 lands and the `is_active` unique constraint is dropped, this endpoint may return ambiguous results (multiple `is_active=true` rows). At that point either deprecate it or repurpose to return a list. Defer that decision to Phase 2.
- README + CLAUDE.md anti-pattern note "Carrying context across a project switch" still applies but its meaning shifts: a "project switch" now means "the user starts a new Claude Code session with a different name" rather than "user PATCHes is_active mid-session." Explicit mid-session switches are still possible (user says "actually let's switch to myapp") and Lead must re-bootstrap from scratch.
- Standards-zone files (`postgresql/soft-delete.md`, `fastapi/atomic-mutations.md`, `fastapi/routing.md`, `sqlalchemy/migrations.md`) currently cite agent-teams's `ux_projects_active_one` as a worked example of the partial-unique-on-soft-delete pattern. After Phase 2 lands, those references need a humans-only update — note that the example was historically valid + point at a different (still-current) project example or invent a synthetic one. Surfaced now so it doesn't ship stale.
- Multi-project safety is now a **behavioral protocol** (CLAUDE.md text + user discipline) rather than a DB-enforced invariant. **This is context-based — vulnerable to compaction.** **Phase 3 (locked, filed as separate Kanban task)** moves the gate to the **API layer via mandatory `X-Project-Id` header** — every task-touching endpoint (`GET/PATCH/DELETE /api/tasks/{id}`, `GET /api/tasks?...`, etc.) requires the header; the API verifies that the resource's `project_id` matches the header value. Mismatch / missing → 400 with stable detail. Effect: when Lead's working memory loses the session-bound project (compaction), the next task-API call FAILS at the wire, forcing Lead to re-bootstrap (re-ask user) instead of silently writing to the wrong project. The user explicitly chose header over query-param because URLs stay cleaner and the `X-Project-Id: N` value can be set once via curl alias / per-spawn convention. Trade-off: header isn't visible in raw URL transcripts, but curl `-H` flags ARE recorded in the Bash command text, so transcripts still capture the binding.

**Cross-references:**
- Audit findings: chat 2026-05-09 (this session) "Audit 7 layers" enumerated multi-project Gap A (multi-terminal global state collision).
- Related: `context/teams/dev/decisions.md` 2026-05-09 'Kanban-driven AI: 2-mode model + per-project consent gate' — Mode A2 / B both assume session-scoped resolution implicitly; this entry makes it explicit.
- Phase 2 Kanban task: **#694** (drop unique constraint + redefine is_active + tests + standards proposals).
- Phase 3 Kanban task: **#695** (mandatory `X-Project-Id` header — closes the compact-vulnerability of Phase 1's behavioral protocol). Recommended to ship BEFORE #694 since the hard binding makes #694 safer.

## 2026-05-09 — Kanban-driven AI: 2-mode model + per-project consent gate (Step 2 architecture, design lock — Kanban #481)
**Scope:** team-playbook / agent-prompts / lifecycle / cross-team automation surface
**Proposed by:** user (vision); lead (4-question design lock chat)
**Decision:** Codify the architecture for letting Kanban tasks drive AI work. Two co-existing modes; **per-project consent** gates the dangerous one. The model applies to **every** `team='dev'` project (and any future team) that wants Kanban-driven AI execution — not just the agent-teams dogfood project.

**Mode A2 — user-in-loop + auto-pickup.** User keeps Claude Code (CLI/IDE) open. A queue runner inside the Claude Code session polls the API, picks the next eligible task per ordering rules (priority + parent in_progress + assigned_role match + `process_status=1` + task `run_mode='auto_pickup'`), and dispatches it to Lead. Per-Write/Edit/Bash approval prompts stay exactly as today. Runner location locked = **Claude Code skill / slash command** (e.g. `/run-queue`) using `ScheduleWakeup` or `/loop` primitives — reuses the harness's context engine (CLAUDE.md auto-load, subagent spawning, settings.json approval gating) without standing up new infra. Per Q0-Q2: the skill itself is **Lead-meta tooling** (lives in `~/.claude/skills/` or equivalent), not a project artifact.

**Mode B — Kanban-only headless.** UI button or backend cron triggers a separate **worker service** (own container in `docker-compose.yml`, own image with `claude` CLI installed) that executes the task with `claude -p` in headless mode — no per-Write approval prompts; pre-allowlisted tool surface. Worker host locked = **separate worker service**, NOT the `api` container — keeps Python API image small, lets the worker scale / restart / be rate-limited independently of the API, and isolates blast radius if a headless run goes rogue. Trade-off accepted: one extra docker-compose service vs single-image simplicity.

**Per-project consent gate (replaces team-based gate).** Mode B is **not** restricted by team. Originally proposed as "team='dev' blocks Mode B because Edit on code is destructive" — rejected because blast radius is per-project, not per-team (a novel project might rewrite chapters destructively; a dev sandbox project might be safe). Replacement: each project must have an explicit `auto_run_consent_at TIMESTAMPTZ` in its row. Default = NULL (no consent → no Mode B tasks may run in that project, full stop). User grants per project via UI flow.

**Consent UX locked = typed acknowledgment.** Granting consent requires the user to type the project name verbatim (or a project-specific phrase) — not a single button. Reasoning: Mode B unlocks unattended `Edit` / `Bash` on the project's filesystem; the friction of typing forces a deliberate keystroke vs accidental click on a single-button modal. Same pattern as GitHub's "type the repository name to delete" — a deliberate-action UX for irreversibly-consequential operations.

**Migration 0005 scope locked: timestamp-only, no `consent_text` column.** The migration adds `tasks.run_mode` enum 3-state (`manual` default / `auto_pickup` / `auto_headless`) and `projects.auto_run_consent_at TIMESTAMPTZ NULL`. NO `auto_run_consent_text` — keeps the schema minimal; if the warning text changes later and we want to expire consent, the UI layer can version it (compare against a constant in TS) without a schema change. App-layer cross-table validator: `task.run_mode = 'auto_headless'` is valid only if `project.auto_run_consent_at IS NOT NULL` (lives in `api/src/services/` or the POST/PATCH router; not a single-column CHECK because it spans tables).

**Sequencing: Step 2 prep lands BEFORE Phase 3 V2 (#406).** Originally Step 2 was scheduled after V2; user pivoted to land schema + API + UI seams first so V2's read-only board surfaces `run_mode` badges and consent state from day 1 — avoids UI refactor after Step 2 ships. Tracked as **Kanban #481** (priority=3 high, no parent — separate umbrella from the #3 Phase 3 UI umbrella).

**Reasoning:** The two-mode split exists because the user-base is two-mode: developers want approval prompts (their Edit blast radius is high but they want to verify); non-developers want one-click execution (their tasks are content-shaped — research, summarization, prose). One mode for both fails both: forcing prompts on non-coders ruins the use case; removing them for coders breaks the safety model that the entire `.claude/settings.json` permission policy is built around. The per-project consent gate is the right abstraction because the project owner is the only person who can answer "is the blast radius in this project acceptable for unattended execution?" — neither Lead nor the team taxonomy has that information. Worker-service-not-api-container is the standard async-worker pattern; the API container's job is request/response, not long-running subprocess management. Typed acknowledgment is the de-facto safety UX for irreversible-ish actions; the "single I-accept button" was rejected because every web user has muscle-memory clicked through such modals at least once. Timestamp-only migration is the YAGNI default; consent_text was the speculative-defensive addition that we can add later if a real incident demands it.

**Implications:**
- New schema: `tasks.run_mode` + `projects.auto_run_consent_at` (Migration 0005, lands as #481 subtask).
- New API: `POST /api/projects/{id}/grant-consent` (typed-name body); `PATCH /api/tasks/{id}` accepts `run_mode`; `ProjectRead` + `TaskRead` extended.
- V2 board MUST surface `run_mode` badge per task card + consent state banner per project (read-only — actual grant flow lives in V3+).
- Standards-propagation queue grows: (a) "long-running subprocess work belongs in a worker service, not the API container" (`fastapi/architecture.md` or `docker/compose.md`); (b) "irreversibly-consequential UX requires typed acknowledgment, not a single button" (`general.md` UX patterns or `react/general.md`); (c) cross-table validators belong in service layer / endpoint, not as DB CHECK (`sqlalchemy/orm.md` or `postgresql/operations.md`). Surfaced; humans-write.
- Open at #481 kickoff: subtask breakdown — proposing 3 children (migration / backend / frontend types+UI). Decision entry into agent-teams's `shared/decisions.md` will land per-subtask, capturing implementation specifics (constraint names, endpoint exact shape, UI component boundaries).
- Mode A2 skill itself (`/run-queue`) is out of scope for #481 — that's a Step 2 implementation task, NOT Step 2 prep. #481 only lays the schema/API/UI seams; the runner skill ships when we actually wire Mode A2.

## 2026-05-09 — Codify Q0-Q2 placement framework + named zones + dogfood-pollution lesson (Phase 2.5c)
**Scope:** team-playbook / bucket-architecture / lessons / context architecture
**Proposed by:** lead (closing forecast from Phase 2.5b1 + 2.5b2 entries)
**Decision:** Three coordinated doc edits land the MD-compaction conclusions as durable rules instead of session memory. (1) **CLAUDE.md `## Storage architecture`** rewritten — drop Bucket 1/2/3/4 numbering in favor of five named zones (DB / Standards / Team methodology / Project shared / Role state) with a Writer / Read-scope / Blast-radius column triple, then a new **Q0-Q2** placement framework (Q0 transactional? → DB; Q1 universal? → Standards; Q2 per-team-cross-project? → Team methodology; otherwise project-scope split shared/role). The "push up the zone hierarchy when in doubt" anti-pattern is included inline with a forward-pointer to the lessons.md 3-strikes section. (2) **`.claude/docs/context-layout.md`** tree comments retitled from "Bucket 2/3/4" to "Standards zone" / "Team-methodology zone" / "Project-shared zone" / "Role-state zone"; the `(Bucket 1 = DB)` parenthetical was dropped (covered in CLAUDE.md). The DB-zone-lives-in-PostgreSQL note moved to the file's lede so the tree no longer needs to mention it. Write/read matrix already path-keyed — unchanged. (3) **`.claude/docs/lessons.md`** got a new `## Dogfood-pollution: 3-strikes pattern` section listing all three strikes (smoke-checklist Phase 2 → bb17287, decisions.md Phase 2.5a → ba61349, lead-column Phase 2.5b1 → 3b03ffa) with the "content placed where first used, not where logically belongs" diagnosis and the "would a new project under this team need this?" forward-check rule. Stale `lead='dev'` / `active lead's roster` refs in earlier sections fixed. The "shared/ is Lead-only" lesson extended to "shared/ AND teams/ are Lead-only".
**Reasoning:** Phase 2 / 2.5a / 2.5b1 each fixed one strike of the same underlying mistake — content placed by use-site convenience, not by scope-bound zoning. Without a forward-readable rule, Phase 3 (and every future contributor) will replay the trap. Naming the zones (instead of numbering them) makes a placement decision a single sentence: "this is **Team methodology**" beats "this is Bucket 3" — the noun carries meaning. Q0-Q2 codifies the "first yes wins" gating that was applied implicitly across the three strikes. Bucket numbering also broke down at 5 zones (DB + 4 MD zones); the named approach scales when a future zone is introduced.
**Implications:** New rule: every placement decision walks Q0-Q2 BEFORE the file is opened — not after the file is half-written and the reviewer flags drift. Tier-1 / Tier-2 reports may now cite zone names (e.g., "this insight belongs in Standards zone" → humans-only proposal). README.md was extended in the same commit: ASCII flow diagram + the two trees (`context/` layout + repo top-level) replaced "Bucket N" with zone names AND added the previously-missing `teams/` zone (the bb17287 / 3b03ffa / 2deac05 sequence had updated CLAUDE.md + context-layout.md but not README; this closes the gap). Phase 2.5 series fully closed. Next functional task: **Phase 3 V2 (#406 read-only Kanban board)** — `web/lib/api.ts` typed fetch helpers + replace placeholder page with 5-column board by `process_status`. Dogfood-pollution prevention is the durable check; the lessons.md section is the canonical reference for future Lead instances faced with strike #4 candidates.

## 2026-05-09 — Rename `.claude/leads/` → `.claude/teams/` and `context/leads/` → `context/teams/` (Phase 2.5b2)
**Scope:** team-playbook / agent-prompts / bucket-architecture / context architecture
**Proposed by:** lead (mechanical follow-up to Phase 2.5b1 column rename — forecasted in `agent-teams/shared/decisions.md` 2026-05-09 entry)
**Decision:** Doc-only rename to align directory paths with the column name now that `projects.lead → projects.team` (Phase 2.5b1) made the meaning concrete: a project's `team` field selects the team of agents that owns it, and the playbook + per-team methodology folder follow the same noun. `git mv` preserves history. Live pointers updated in CLAUDE.md, README.md, `.claude/teams/{dev,novel}.md`, `.claude/agents/dev-{tester,reviewer}.md`, `.claude/docs/{context-layout,spawn-template}.md`, `context/teams/dev/decisions.md` header, `context/projects/agent-teams/shared/{decisions,db-schema,smoke-matrix,release-matrix}.md`. Historical decision-entry bodies (this file's earlier 2026-05-09 / 2026-05-08 entries; `agent-teams/shared/decisions.md` 2026-05-09 #Phase-2.5b1 + earlier 2026-05-08 entries) keep their `context/leads/` / `.claude/leads/` strings unchanged — append-only log convention; date-stamps make them readable as historical state. `.claude/settings.json` allowlist drops `Write/Edit(context/leads/**)` (added proactively in `6159983`); `Write/Edit(context/teams/**)` already present. No schema, no migration, no API contract change. Pytest 69/69 GREEN, `GET /api/projects/active` returns `team:"dev"` unchanged.
**Reasoning:** "Lead" (the meta-orchestrator persona, capital L) and "team" (the column) were both encoded into `.claude/leads/` / `context/leads/` paths, blurring the distinction the Phase 2.5b1 rename had just clarified at the column level. Aligning paths makes the mental model crisp: capital-L Lead is a persona; lowercase team is data. The dogfood-pollution lesson held — same as smoke-checklist Phase 2 and decisions.md Phase 2.5a, the live pointer must be updated while the historical narrative stays stamped.
**Implications:** Phase 2.5c (codify decision framework Q0-Q2 + zone names + dogfood-pollution lesson + 3-strikes anti-pattern in CLAUDE.md / context-layout.md / lessons.md) is the only MD-compaction phase remaining before Phase 3 V2 (#406 read-only Kanban board). Future leads scaffolded via `.claude/teams/<name>.md` + `context/teams/<name>/` get the renamed locations; old `leads/` paths no longer exist. Allowlist refresh: `Write/Edit(context/leads/**)` removed (the directory no longer exists; rule was a Phase 2.5b2 stopgap).

## 2026-05-09 — Split `smoke-checklist` / `release-checklist` into methodology (cross-project) + matrix (per-project) — Bucket 4 introduced
**Scope:** bucket-architecture / shared / .claude / context architecture
**Proposed by:** user (during MD compaction Phase 2 — observation: "shared/ มันเป็นของ agent type มากกว่า project")
**Decision:** Introduce **Bucket 4** = `context/leads/<lead>/` — Lead-writable, cross-project per-lead methodology. Sits alongside existing buckets: 1 (DB), 2 (`context/standards/` humans-only), 3 (per-project shared, now formally Bucket 4 in CLAUDE.md numbering). Created `context/leads/dev/smoke-methodology.md` (Tier-1 probe shape, decision matrix, POSITIVE+NEGATIVE rule, restoration discipline, output convention, Kanban #76 worked example, web smoke matrix) and `context/leads/dev/release-methodology.md` (Tier-2 trigger conditions, pre-flight, Step 1-5 patterns, severity scales, wrap-up summary template). Renamed agent-teams's per-project files: `shared/smoke-checklist.md` → `smoke-matrix.md` (now ~50 lines: endpoints/hosts, project-specific trigger paths, canonical seed values, past-escape cross-references) and `shared/release-checklist.md` → `release-matrix.md` (now ~60 lines: endpoint matrix table, Step 4 dep-audit context, Step 5 audit-log queries with agent-teams DB names). CLAUDE.md updated: golden rule extended (Lead writes `context/leads/<lead>/*` too; subagents propose); storage architecture table grew to 4 buckets. `.claude/leads/dev.md` step 5b + Release-wrap-up Step 2 references updated to point at methodology + matrix split. `.claude/agents/dev-tester.md` + `dev-reviewer.md` updated to read methodology FIRST then matrix. `.claude/docs/context-layout.md` updated tree + write/read matrix.
**Reasoning:** Methodology is universal across dev projects (probe shape, severity scales, anti-patterns) but currently lived in `agent-teams/shared/` only. New dev projects scaffolded via POST /api/projects got 3 template files (decisions/api-contracts/db-schema) but NOT smoke-checklist/release-checklist — methodology was inaccessible to them. Lifting methodology to a cross-project per-lead zone solves three problems at once: (a) **runtime efficiency** — Lead loads methodology lazily (only when task triggers smoke/release), saving ~150 lines of context on the ~80% of sessions that don't need it; (b) **edit safety** — single-concern files vs ~250-line mixed lifecycle+methodology playbook reduces accidental cross-concern edits; (c) **lead scaling** — adding novel/data-science/etc. leads tomorrow doesn't bloat their playbook with their own methodology files. User explicitly weighted "accumulate effort during operations > upfront effort" when choosing this architecture.
**Implications:** dev-tester / dev-reviewer spawn prompts now inject TWO files (methodology + matrix). Future dev projects scaffolded via POST /api/projects will read methodology by reference (no copy needed); a follow-up may add per-lead scaffold templates so new projects get a stub `smoke-matrix.md` / `release-matrix.md` with their own endpoints — currently they would need to author these manually (acceptable; matrix is small ~50 lines). Historical decisions.md entries (#78 #80 below) reference `smoke-checklist.md` / `release-checklist.md` by old name — those are factually correct as of their dates and intentionally left unchanged (append-only log). The `backfill-2026-05-08.md` event report refs were patched inline since it's a closed nav-document, not an event log. **Open questions:** (1) should novel projects get a `context/leads/novel/` folder pre-seeded? deferred until first novel project lands; (2) should `api/src/services/project_scaffold.py` dispatch per-lead matrix stubs on POST /api/projects? flagged but out-of-scope for this commit.

## 2026-05-08 — Release wrap-up Tier-2 gate + dev-reviewer security mode — Kanban #80
**Scope:** lead-playbook / agent-prompts / shared
**Proposed by:** lead (gating decision: 'use existing skills' — extend dev-reviewer instead of creating dev-security agent)
**Decision:** Release-wrap-up flow is now an explicit Tier-2 gate triggered by a Kanban task titled `release wrap-up <version>` or `publish wrap-up <version>` (or manual user signal in chat). `.claude/leads/dev.md` got a new top-level `## Release wrap-up flow (Tier-2 gate before publish)` section between the Lifecycle and Anti-patterns sections, codifying 7 sequential steps: (1) pre-flight queue check (no `process_status=2/4` rows allowed), (2) full Tier-1 smoke matrix (all endpoints — extends per-task Tier-1 from #78), (3) `/security-review` slash command (USER-TRIGGERED — Lead documents the request, user fires it manually), (4) dev-reviewer in `mode: security`, (5) `pip-audit` dependency CVE audit, (6) audit-log review on `tasks_history` + projects `updated_at` differential, (7) wrap-up summary `PATCH`ed back into the Kanban task description with GREEN/YELLOW/RED per step. Anti-pattern: running wrap-up in same session as last feature commit → context pollution; open fresh session. New shared template `release-checklist.md` (Lead-only writer) carries the endpoint matrix (POSITIVE+NEGATIVE probe pairs per endpoint), the security-mode audit surface (input validation / authn-authz / SQLi / CSRF / secret leakage / dep CVE / error-disclosure), the SECURITY-* severity scale (BLOCKER/WARN/NIT/KNOWN-GAP — distinct from default review BLOCKER/major/minor/nit), the `pip-audit` severity gate, the audit-log SQL templates, and the wrap-up summary template. `.claude/agents/dev-reviewer.md` got a new `## Security mode (release wrap-up only)` section codifying audit surface + severity scale + `security-mode-review-<date>.md` output path + refuse-on-non-release-task anti-pattern. *Note 2026-05-09:* `release-checklist.md` was later split into `release-methodology.md` (this folder) + per-project `release-matrix.md` — see 2026-05-09 entry above.
**Reasoning:** v0.x has no security gate before publishing. Tier-1 smoke (#78) catches per-task regressions; Tier-2 is the explicit, expensive, rare gate that runs ONLY when releasing. Decision per 2026-05-08 chat: 'use existing skills' route — leverage the built-in `/security-review` slash command (free, user-triggered) plus extend dev-reviewer with a security-mode clause, rather than creating a dedicated `dev-security` agent. Defer dev-security agent until codebase has auth surface that justifies a dedicated role (currently NONE — auth is Phase 4). dev-reviewer's prompt was already only ~100 LOC; adding security mode keeps the file readable and concentrates the security knowledge in one place. The SECURITY-* severity scale is intentionally distinct from default review tags so wrap-up history is filterable across the dev-reviewer/ folder by file-name pattern (`security-mode-review-*.md` vs `review-*.md`).
**Implications:** Future releases follow the release-methodology + project-specific release-matrix. dev-reviewer can be spawned in two modes — default (per-task correctness/style) and security (release-only); the latter refuses on non-release tasks. `pip-audit` is not yet installed in the api container — first wrap-up will install it as a dev dep (one-time setup; will propose adding to `requirements-dev.txt` or pyproject.toml). Authn/authz is documented as SECURITY-KNOWN-GAP for v0.x — first Tier-2 wrap-up will tag it explicitly so the release ships with documented gap rather than accidental omission. Pure process / docs / agent-prompt change — no production code, no schema, no pytest delta. Pairs with #78 (Tier-1) + #79 (regression discipline): #78 catches per-task at deploy-verify; #79 catches at PR-fix; #80 catches at release-wrap-up. Discipline-rollout chain remaining is now #81 (backfill sweep on current branch — see agent-teams's project decisions.md).

## 2026-05-08 — Regression-test discipline (fail-before/pass-after rule) — Kanban #79
**Scope:** agent-prompts / standards proposal
**Proposed by:** lead (after #76 manual application proved the discipline)
**Decision:** Encode the fail-before/pass-after regression-test rule into agent prompts. `.claude/agents/dev-tester.md` got a new `### 2a. Regression test discipline (BLOCKER / MAJOR fixes)` section requiring: (a) the regression test demonstrably FAILS on pre-fix code AND PASSES on post-fix code, (b) both pytest transcripts captured verbatim in a `## Regression demo` section of the final report, (c) `git stash push -- <fix-paths>` workflow to round-trip the production fix while keeping tests in working tree, (d) `# Regression: Kanban #<n>` pin comment (Kanban id only — no commit SHAs that rot), (e) anti-pattern note forbidding bare `actual == baseline` against immutable baselines. The discipline applies only to BLOCKER/MAJOR fixes; feature tests keep the existing golden-path/edges/errors/boundaries workflow. `.claude/agents/dev-reviewer.md` got a parallel audit clause under `### 2. Review` — missing `## Regression demo` section on a BLOCKER/MAJOR task = BLOCKER, vacuous-shape assertion = WARN.
**Reasoning:** Kanban #76 demonstrated that regression tests can pass for the wrong reason — the M9 test asserted `updated_at_after == updated_at_before` after a re-DELETE, but neither side bumped, so equality held trivially. The fix at the test-discipline layer is to mandate the fail-before transcript: dev-tester `git stash`es the production fix and re-runs pytest, capturing the FAIL output before popping. The `git stash` mechanic naturally exercises the "the test must actually probe the bug" invariant — if stashing the fix doesn't break the test, the test isn't load-bearing. Manual application to #76 (in the same session that produced this discipline) caught two strengthened tests failing at exactly the load-bearing assertions on pre-fix code (`tests/test_routes_smoke.py:704` + `:761`); both passed after stash-pop. The evidence base is documented in agent-teams/shared/decisions.md 2026-05-08 #76 entry and serves as the worked example future readers cite.
**Implications:** Every future BLOCKER/MAJOR fix lands with a `## Regression demo` section in dev-tester's report. dev-reviewer enforces. Cost is near-zero at runtime (regression tests live in the existing pytest suite); cost only at fix time is one `git stash` round-trip per fix (~10s). Pairs with Kanban #78 (Tier-1 smoke at deploy-verify): #78 catches at deploy, #79 catches at PR-fix time. Pure agent-prompt change — no production code, no schema, no pytest delta. Standards proposal (humans-only writers) for `context/standards/general.md` Testing section: codify the fail-before/pass-after rule at the project-wide convention level — surfaced here, not auto-edited.

## 2026-05-08 — Tier-1 smoke probe step in dev lifecycle + smoke-checklist template (Kanban #78)
**Scope:** lead-playbook / agent-prompts / shared
**Proposed by:** lead (after #76 escape diagnosis)
**Decision:** Insert lifecycle step 5b in `.claude/leads/dev.md` between current steps 5 and 6: when a task touches `api/src/routers/`, `api/alembic/versions/`, `api/src/schemas/`, `api/src/models/`, `api/src/templates/`, `docker-compose.yml`, env files, or `api/src/main.py`, Lead spawns dev-tester to run scoped `curl localhost:8456` probes against the running container with **behavior assertions** (not just HTTP status codes). New shared template `smoke-checklist.md` (Lead-only writer) holds the decision matrix (which paths trigger Tier-1 vs skip), the probe boilerplate (POSITIVE + NEGATIVE pair shape), the anti-pattern note (vacuous `actual == baseline` shape forbidden — must pair with sibling positive-path assertion), restoration discipline (production rows restored; throwaway rows soft-deleted before return), and the worked example from Kanban #76 (probes A-E). Two agent prompts updated to enforce: `dev-tester.md` got a new `### 2b. Tier-1 smoke probe (live API)` subsection codifying the workflow + the mandatory `## Tier-1 smoke probe results` report section; `dev-reviewer.md` got a Tier-1 audit clause under `### 2. Review` — missing section on a router-touching task = BLOCKER, vacuous-shape assertion = WARN. Cost target: 1-3 probes per task, < 30 seconds total wall-clock. Tier-2 (full API + security + dep audit) stays out of scope — that is Kanban #80. *Note 2026-05-09:* `smoke-checklist.md` was later split into `smoke-methodology.md` (this folder) + per-project `smoke-matrix.md` — see 2026-05-09 entry above.
**Reasoning:** Kanban #76 (projects.updated_at) escaped because (a) the M9 regression test passed for the wrong reason — `actual == baseline` was vacuously true since neither side bumped — and (b) no lifecycle step probed the live API end-to-end after a change landed in the working tree. dev-tester wrote pytest assertions; dev-reviewer audited source; nobody curl'd localhost. Adding step 5b is the minimum process change that catches M9-class vacuous-assertion bugs at deploy-verify without waiting for the regression-test-discipline rollout (Kanban #79). The cost is intentionally low so the step is run every applicable task; richer probes belong in Tier-2 release wrap-up.
**Implications:** Every future task touching the trigger paths above must include a `## Tier-1 smoke probe results` section in dev-tester's report with at least one POSITIVE + one NEGATIVE behavior assertion. dev-reviewer audits for the section. The discipline is now self-documenting via the worked-example pointer to Kanban #76 — future readers can see the canonical probe shape (Probes A-E by dev-backend + Lead's independent no-op probe) without reconstructing it from incident notes. Pure process / docs / agent-prompt change — no production code, no schema change, no pytest delta. Pairs with Kanban #79 (regression-test fail-before/pass-after discipline): #78 catches at deploy-verify; #79 catches at PR-fix time. Tier-2 (Kanban #80) builds on #78's conventions as the release-wrap-up superset. The discipline-rollout chain remaining is now #79 → #80 → #81.

<!-- No earlier methodology decisions. First entry will be appended above this line. -->
